[project]
name = "low-level-design"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ritwik Prasad", email = "ritwikprasad142@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
low-level-design = "low_level_design:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
